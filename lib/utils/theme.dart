import 'package:flutter/material.dart';

ThemeData getflutter_playgroundTheme(ColorScheme colorScheme) => ThemeData(
      useMaterial3: true,
      colorScheme: colorScheme,
      fontFamily: 'Regular',
    );
